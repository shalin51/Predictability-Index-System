// Re-exports from @amfpi/shared for dashboard use.
// Import from here or from '@amfpi/shared' directly — never define your own API types.
export type {
  HealthResponse,
  DbHealthResponse,
  VersionResponse,
  ApiSuccessResponse,
  ApiErrorResponse,
  PaginatedResponse,
} from '@amfpi/shared';
